<Window
    x:Class="SimpleGraphGenerator.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:myControls="clr-namespace:SimpleGraphGenerator.Views"
    Title="MainWindow"
    Width="1100"
    Height="800"
    mc:Ignorable="d">

    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <Label
                    HorizontalAlignment="Center"
                    Content="Simple Graph Generator"
                    FontSize="25" />
                <Separator Margin="5" />
                <StackPanel
                    Margin="0,20,0,20"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <TextBlock FontSize="15">
                        Liczba wierzchołków:
                        <TextBlock Text="77" />
                    </TextBlock>
                    <TextBlock Margin="20,0,0,0" FontSize="15">
                        Liczba krawędzi:
                        <TextBlock Text="77" />
                    </TextBlock>

                </StackPanel>

                <Separator Margin="5" Opacity="0.25" />

                <Label
                    HorizontalAlignment="Center"
                    Content="Prawdopodobieństwo wystąpnienia krawędzi "
                    FontSize="12"
                    FontStyle="Italic" />
                <!--  TODO write value converter  -->


                <Slider
                    Name="sliderProb"
                    Margin="10,0,10,0"
                    IsSnapToTickEnabled="True"
                    Maximum="1"
                    Minimum="0"
                    TickFrequency="0.01"
                    TickPlacement="None" />



                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="20"
                    Foreground="PaleVioletRed">
                    p =
                    <TextBlock Text="{Binding ElementName=sliderProb, Path=Value, UpdateSourceTrigger=PropertyChanged}" />
                </TextBlock>

                <Button
                    Margin="10,40,10,0"
                    Padding="5"
                    Content="Generuj graf"
                    FontSize="15" />

            </StackPanel>

            <myControls:Scene x:Name="MyScene" Grid.Column="1" />



        </Grid>



    </Border>

</Window>
